/*
This server file organizes and exposes the submodules for the server application. It includes modules
for server logic, return code handling, and shared declarations.
 */

pub mod server;
pub mod return_wrapper;
pub mod server_declarations;
